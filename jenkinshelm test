pipeline {
    agent {label 'slave1'}
    stages {
        stage('my Build') {
            steps {
                sh 'docker build -t tomcat_build:${BUILD_NUMBER} --build-arg BUILD_NUMBER=${BUILD_NUMBER} .'
            }
        }  
        stage('publish stage') {
            steps {
                sh "echo ${BUILD_NUMBER}"
                withCredentials([usernamePassword(credentialsId: 'dockerHub', passwordVariable: 'dockerHubPassword', usernameVariable: 'dockerHubUser')]){
                sh 'docker login -u ${dockerhubUSER} -p ${dockerhubPassword}'
                sh 'docker tag tomcat_build:${BUILD_NUMBER} sangmeshbhure/bhuretest:${BUILD_NUMBER}'
                sh 'docker push sangmeshbhure/bhuretest:${BUILD_NUMBER}'
                }
            }
        } 
    } 
}         
